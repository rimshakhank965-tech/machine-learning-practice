from sklearn.ensemble import RandomForestRegressor 
from sklearn.metrics import mean_absolute_error, mean_squared_error
import numpy as np 
features = ["op1", "op2", "op3",            
            "sensor_2", "sensor_3", "sensor_4",            
            "sensor_7", "sensor_11", "sensor_12"] 

X = train[features] 
y = train["RUL_capped"]

model = RandomForestRegressor(    
    n_estimators=100, random_state=42 
)

model.fit(X, y) 

predicted_rul = model.predict(X) 

mae = mean_absolute_error(y, predicted_rul) 
rmse = np.sqrt(mean_squared_error(y, predicted_rul))

print("Random Forest RUL Model") 
print("MAE :", mae) 
print("RMSE:", rmse)
